import React from 'react'

const ShoppingList = () => {
    return (
        <div>
            recipes.ingredients is gonna have to be an array and then mapped, and set to state to be recombined to be shown in Ingredients.
            Shopping list will add new ingredients to an object
        </div>
    )
}

export default ShoppingList